import { SLIDE_WIDTH, SLIDE_HEIGHT } from '@imprime/sdk'

export { SLIDE_WIDTH, SLIDE_HEIGHT }

// Frontend-specific display constants
export const EDITOR_DISPLAY_WIDTH = 1000;
export const EDITOR_DISPLAY_HEIGHT = Math.round((EDITOR_DISPLAY_WIDTH * 9) / 16);
